# Non-secret config — safe to commit
# Secrets are in .dev.vars (local) or wrangler secrets (production)

PLAID_ENV=production
SESSION_COOKIE_NAME=finance_monitor_session
NEXTAUTH_URL=http://localhost:3000
